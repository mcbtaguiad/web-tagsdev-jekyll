name: Docker Image CI

on:
  push:
    branches: [ "c" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag ghcr.io/mcbtaguiad/web-tagsdev-jekyll:$(GITHUB_REPOSITORY)